<table> All Courses
  <tr><th>Name</th><th>Actions</th><tr>
    <% @courses.each do |course| %>
    <tr><td><%= course.name %></td><td><%=link_to "Show", course_path(course) %></td></tr>
    <% end %>
</table>

<h3><%= link_to "New Course", new_course_path %></h3>